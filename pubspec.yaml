name: crossword_multiplayer_game
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/clues/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
